[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "Titan2_OS"
version = "0.1.0"
description = "Titan 2.0 Robotics Operating System"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
include = ["titan*"]